import type { AgentRunResult, AgentStreamEvent } from '../agent/types.js';

export function renderHelp(): string {
  return `nju-agent - a local coding agent\n\nUsage:\n  nju-agent [options] [prompt]\n  nju-agent --print "explain this repo"\n  nju-agent --mode json "explain this repo"\n\nOptions:\n  -h, --help                       Show this help message\n  -v, --version                    Show version\n  -p, --print <prompt>             Run one prompt in human-readable mode\n      --mode <text|json|rpc>       Select run mode (default: text)\n      --json                       Shortcut for --mode json\n      --rpc                        Shortcut for --mode rpc\n      --api-format <format>        openai-chat, openai-responses, or anthropic\n      --model <id>                 Override NJU_AGENT_MODEL\n      --base-url <url>             Override NJU_AGENT_BASE_URL\n      --api-key-env <name>         API key env var (default: NJU_AGENT_API_KEY)\n      --cwd <path>                 Workspace root\n      --session <id>               Resume a session\n      --no-session                 Do not persist this conversation\n      --permission-mode <mode>     yolo, strict, or confirm (default: yolo)\n      --approve                    Trust current workspace for this run\n      --no-approve                 Do not load trust-gated project resources\n      --telemetry <mode>           off, normal, or debug\n\nInteractive TUI (run without a prompt):\n  /help                            Show command summary\n  /new                             Start a new session\n  /trust                            Trust the current workspace for future runs\n  /name <name>                     Name the current persisted session\n  /rename <session_name>            Rename the current persisted session\n  /fork                            Fork the current session\n  /sessions                        List sessions\n  /session                         Show current session/model/effort\n  /resume [id]                     Open session picker, or resume id\n  /model [id]                      Open model picker, or set model id\n  /effort [level]                  Open effort picker, or set model thinking effort\n  /reasoning [on|off]              Open display picker, or show/hide reasoning deltas\n  /thinking [on|off]               Alias for /reasoning\n  /memory                          Show local persistent-memory status\n  /compact                         Append a deterministic summary of current session history\n  /status                          Show latest run evidence and verification status\n  /diff                            Show tracked file changes for the current session\n  /undo                            Undo the latest hash-protected tracked file change\n  /reload                          Reload user plugins for the next run\n  /quit, /exit                     Exit TUI\n\nEnvironment:\n  NJU_AGENT_API_FORMAT             API protocol (openai-chat, openai-responses, anthropic)\n  NJU_AGENT_API_KEY                API key (never pass secrets as CLI args)\n  NJU_AGENT_BASE_URL               OpenAI-compatible base URL\n  NJU_AGENT_MODEL                  Model id\n  NJU_AGENT_THINKING_LEVEL         Initial effort: off, minimal, low, medium, high, xhigh, max\n  NJU_AGENT_TOOL_PREVIEW_LINES      Preview head lines (default: 8, range: 1-100)\n  NJU_AGENT_THINKING_LEVEL_MAP     JSON map; null disables a level\n  NJU_AGENT_THINKING_FORMAT        reasoning_effort, anthropic-adaptive, or anthropic-budget\n  NJU_AGENT_MAX_DURATION_MS         Run deadline, default 600000 (1–1800000)\n  NJU_AGENT_MAX_TOOL_CONCURRENCY    Parallel tool limit, default 4 (1–8)\n  NJU_AGENT_MEMORY_ENABLED         true/false (default: true)\n  NJU_AGENT_MEMORY_DIR             Optional absolute local memory directory\n\nDuring a run, Esc or Ctrl+C cancels. Enter queues the current draft; Ctrl+Enter steers the active run.\nCtrl+O toggles the latest tool result details.\nStrict/confirm tool approvals use Enter/y to allow and n/Esc to deny.\n`;
}

export function renderVersion(): string { return '0.1.0\n'; }

export function renderStreamEvent(event: AgentStreamEvent, showThinking: boolean): string {
  if (event.type === 'thinking_delta') return showThinking && event.delta ? `\n[thinking] ${event.delta}` : '';
  if (event.type === 'tool_call') return `\n${event.preview ?? `[tool] ${event.toolCall.name}`}\n`;
  if (event.type === 'tool_result') return `${event.result.preview ?? `[tool result] ${event.result.toolName} ${event.result.ok ? 'completed' : `failed: ${event.result.error?.code ?? 'error'}`}`}\n`;
  return '';
}

export function renderRunResult(result: AgentRunResult, includeAssistant = true): string {
  if (!includeAssistant) return '';
  const assistantText = result.messages
    .filter((message) => message.role === 'assistant' && message.content.trim())
    .map((message) => `assistant: ${message.content.trim()}`);
  return assistantText.length ? `${assistantText.join('\n')}\n` : '';
}
