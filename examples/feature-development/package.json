{
  "name": "inventory-reservation-service",
  "private": true,
  "type": "module",
  "scripts": { "build": "tsc", "test": "vitest run", "typecheck": "tsc --noEmit", "demo": "npm run build && node scripts/demo.mjs", "reset": "node scripts/reset.mjs" },
  "devDependencies": { "typescript": "^5.0.0", "vitest": "^4.0.0" }
}
