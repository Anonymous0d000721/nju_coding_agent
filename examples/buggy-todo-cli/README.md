# Buggy Todo CLI Demo

This fixture intentionally contains a failing test. From the repository root, ask nju-agent:

> Read `examples/buggy-todo-cli`, fix the implementation without changing tests, and run its tests.

Expected tool path: inspect files, run `npm test`, edit `src/todo.ts`, run tests again.
